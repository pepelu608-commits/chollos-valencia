# Desactivado: ya no se usan subastas. Se mantiene el fichero sin programacion.
name: (desactivado) subastas
on:
  workflow_dispatch:
jobs:
  nada:
    runs-on: ubuntu-latest
    steps:
      - run: echo desactivado
